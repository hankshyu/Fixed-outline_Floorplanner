int main(){

    int soft_module_count;
    int fixed_moduel_count;
    // reads soft block into a vector<Node*>
    // reads fixed module into a vector <Node*>

    //construct Bstar Tree(), add soft_module
    for(int i = 0; i < )
}