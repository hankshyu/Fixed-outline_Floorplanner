# ======================================================================
#  This Makefile builds the ELF file for 
#
#  2023/06/08
# ======================================================================
CC = $(CCPATH)/$(CROSS)-gcc
LD = $(CCPATH)/$(CROSS)-ld
AR = $(CCPATH)/$(CROSS)-ar
OD = $(CCPATH)/$(CROSS)-objdump
SP = $(CCPATH)/$(CROSS)-strip

PROJ = 


.PHONY: all

all:

